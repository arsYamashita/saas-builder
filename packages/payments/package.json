{
  "name": "@saas/payments",
  "version": "0.1.0",
  "private": true,
  "description": "Hardened Stripe integration primitives (client init, idempotent checkout sessions, verified webhook events) shared across saas-builder and generated templates.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "stripe": "^16.0.0"
  }
}
