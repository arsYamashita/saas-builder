{
  "name": "@saas/supabase-guard",
  "version": "0.1.0",
  "private": true,
  "description": "Serverless-safe rate limiting for saas-builder and generated templates. Wraps @upstash/ratelimit behind dependency-injected bucket limiters so app code configures buckets/policy without depending on a live Redis connection at import time. See README.md for the fail-open/fail-closed contract.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./rate-limit": "./src/rate-limit.ts"
  },
  "peerDependencies": {
    "@upstash/ratelimit": "^2.0.8",
    "@upstash/redis": "^1.37.0"
  }
}
