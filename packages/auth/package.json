{
  "name": "@saas/auth",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Supabase Auth client factories, session lookup, and tenant-scoping (RLS defense-in-depth) guard helpers. Split into /client and /server entrypoints so server-only APIs (next/headers, service-role key) can never enter a Next.js client bundle.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./client": "./src/client.ts",
    "./server": "./src/server.ts"
  },
  "dependencies": {
    "@supabase/ssr": "^0.5.0",
    "@supabase/supabase-js": "^2.45.0",
    "server-only": "^0.0.1"
  },
  "peerDependencies": {
    "next": "^14.2.35"
  }
}
